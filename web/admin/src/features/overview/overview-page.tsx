import { PagePlaceholder } from '../../components/page-placeholder';

export function OverviewPage() {
  return (
    <PagePlaceholder
      title="Overview"
      description="Orientation and jump-off point: domain tiles, recent audit activity, and quick-create actions."
      phase="F5"
    />
  );
}
