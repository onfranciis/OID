import { PagePlaceholder } from '../../components/page-placeholder';

export function AuditPage() {
  return (
    <PagePlaceholder
      title="Audit"
      description="Review security-sensitive identity events."
      phase="F5"
    />
  );
}
