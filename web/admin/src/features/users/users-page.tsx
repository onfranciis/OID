import { PagePlaceholder } from '../../components/page-placeholder';

export function UsersPage() {
  return (
    <PagePlaceholder
      title="Users"
      description="Create users, manage profiles, and control lifecycle status."
      phase="F2"
    />
  );
}
