import { test } from '@playwright/test';

test('health endpoint placeholder', async () => {
  test.skip(
    true,
    'Playwright e2e coverage will start once the app bootstrap is finalized.',
  );
});
